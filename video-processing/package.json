{
  "name": "video-processing",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests configured for function app\""
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.2",
    "@azure/data-tables": "^13.3.0",
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.31.0",
    "@azure/storage-queue": "^12.28.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}
